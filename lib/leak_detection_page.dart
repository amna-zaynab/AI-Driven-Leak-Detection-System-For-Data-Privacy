import 'package:flutter/material.dart';

class LeakDetectionPage extends StatelessWidget {
  const LeakDetectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Leak Detection Coming Soon"),
    );
  }
}
